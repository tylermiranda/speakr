.playwright-mcp/
venv/
__pycache__/
instance/
uploads/
*.db
*.db-journal
__pycache__/
*.pyc
*.log
*.env
.migrate/
project_files.md
*.md
notes.md
docker-compose.yml
changes.txt

!README.md
!CLA.md
!CONTRIBUTING.md
!AGENTS.md
!.github/CLA-SETUP.md
!docs/**/*.md
!.memory/**/*.md
docker-compose.dev.yml
docker-compose.lite.yml
docker-compose.postgres.yml
.clinerules
temp/
.claude/

# Offline vendor dependencies (downloaded during Docker build)
static/vendor/

# Docs build artifacts
docs/_site/
docs/.jekyll-cache/
docs/README.md
site/
.cache/
docs/overrides/.cache/

# Documentation deployment files (examples only)
docs/.github-deploy.yml

# Documentation conversion scripts (one-time use)
scripts/convert_to_mkdocs.py
unraid/

# Frontend test tooling (Vitest)
node_modules/
package-lock.json
pass-*.png
page-*.png
final-*.png

# Session-internal scratch (UX redesign notes etc.). These live in
# temp/ so you can read them locally, but they shouldn't be tracked.
temp/ux-redesign-*.md

# Internal-only documentation (design system, working notes, etc.).
# Lives on disk for local reference but is never tracked.
