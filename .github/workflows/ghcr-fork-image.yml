name: GHCR fork image

# Publishes ghcr.io/tylermiranda/speakr for personal Tower deploys.
# Overlay build (FROM learnedmachine/speakr:latest + COPY app layers) matches
# the Tower Dockerfile.fork path and stays fast. Upstream still publishes
# learnedmachine/speakr via docker-publish.yml (Docker Hub / releases).

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: ghcr-fork-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/speakr

jobs:
  build-amd64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=main-amd64
            type=sha,prefix=,format=short

      - name: Build and push overlay image (amd64)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.fork
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=ghcr-fork-amd64
          cache-to: type=gha,scope=ghcr-fork-amd64,mode=max
